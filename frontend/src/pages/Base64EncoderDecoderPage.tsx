import { BASE64_ENCODER_DECODER_TOOL } from '@/config/tools';
import { ToolPlaceholderPage } from '@/pages/ToolPlaceholderPage';

export function Base64EncoderDecoderPage() {
  return <ToolPlaceholderPage tool={BASE64_ENCODER_DECODER_TOOL} />;
}

export default Base64EncoderDecoderPage;

