import { XML_TO_JSON_TOOL } from '@/config/tools';
import { ToolPlaceholderPage } from '@/pages/ToolPlaceholderPage';

export function XmlToJsonPage() {
  return <ToolPlaceholderPage tool={XML_TO_JSON_TOOL} />;
}

export default XmlToJsonPage;

